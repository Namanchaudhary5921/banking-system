-- Optional demo seed data for the H2 in-memory profile.
-- Spring Boot runs this automatically on startup after Hibernate creates the schema.
-- (Left empty by default so a fresh clone starts clean; uncomment to auto-seed a demo customer.)

-- INSERT INTO customers (first_name, last_name, email, national_id, phone, address, onboarded_date)
-- VALUES ('Jane', 'Doe', 'jane.doe@example.com', 'NID-0001', '555-0100', '123 Main St, Springfield', CURRENT_DATE);
